[phases.setup]
cmds = [
    "apt-get clean",
    "apt-get -o Acquire::Retries=3 update"
]
# Python e edge-tts removidos — TTS agora usa ElevenLabs API (axios, sem CLI)
nixPkgs = ["nodejs_23", "ffmpeg"]

[phases.build]
cmds = [
    "npm install",
    "npm run build"
]

[start]
cmd = "node server.js"