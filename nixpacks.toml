[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_22", "ffmpeg"]

[phases.build]
cmds = [
    "python3.11 -m venv /app/venv",
    "/app/venv/bin/pip install edge-tts",
    "npm install",
    "npm run build"
]

[start]
cmd = "PATH=/app/venv/bin:$PATH node server.js"