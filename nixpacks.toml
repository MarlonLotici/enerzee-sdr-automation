[phases.setup]
nixPkgs = ["chromium", "python311", "python311Packages.pip", "nodejs_23", "ffmpeg"]

[phases.install]
cmds = [
    "npm install",
    "python3.11 -m pip install --user edge-tts"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"