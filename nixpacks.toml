[phases.setup]
nixPkgs = ["google-chrome-stable", "python311", "python311Packages.pip", "nodejs_22", "ffmpeg"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install edge-tts"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"